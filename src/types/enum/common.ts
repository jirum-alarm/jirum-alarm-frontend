export enum ApiType {
  "API",
  "CONNECT",
}

export enum ModalStateType {
  "NONE",
  "ERROR",
}

export enum SortOption {
  NONE = "NONE",
  DESC = "DESC",
  ASC = "ASC",
}

export enum ToastState {
  "success" = "success",
  "danger" = "danger",
  "warning" = "warning",
  "info" = "info",
}
