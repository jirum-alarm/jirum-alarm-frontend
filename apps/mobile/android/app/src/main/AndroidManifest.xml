<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />

  <!-- Android 11+ 패키지 가시성. 공유 시 카톡·X·스레드를 직접 연다. -->
  <queries>
    <package android:name="com.kakao.talk" />
    <package android:name="com.twitter.android" />
    <package android:name="com.instagram.barcelona" />
  </queries>

  <application
    android:name="com.solcode.jirmalam.MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:allowBackup="false"
    android:theme="@style/AppTheme"
    android:supportsRtl="true">

    <!-- OTA(expo-updates). iOS 는 Supporting/Expo.plist 가 같은 역할을 한다.
         ⚠️ 이 레포는 bare(android/ 커밋됨) — EAS 빌드에서 prebuild 도, config plugin 도
         돌지 않는다. 즉 여기 없으면 앱은 URL 을 못 찾아 OTA 가 통째로 꺼진다
         (UpdatesConfiguration.kt:205 가 이 meta-data 를 읽는다).
         ★ expo-channel-name 은 setUpdatesConfigAsync 가 넣어주지 않는다 —
         없으면 서버가 400 을 준다("channel-name: Required"). iOS 에서 이 누락으로
         빌드 3개(23·24·25)가 OTA 를 영구히 못 받았다. 그래서 값을 여기 박는다.
         값의 정본은 app.json(url·runtimeVersion)·eas.json(channel) — 가드 테스트가 대조한다. -->
    <meta-data android:name="expo.modules.updates.ENABLED" android:value="true" />
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_CHECK_ON_LAUNCH" android:value="ALWAYS" />
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_LAUNCH_WAIT_MS" android:value="0" />
    <meta-data android:name="expo.modules.updates.EXPO_UPDATE_URL" android:value="https://u.expo.dev/ec89e7ad-0f52-4777-a71c-50e5f596be58" />
    <meta-data android:name="expo.modules.updates.EXPO_RUNTIME_VERSION" android:value="@string/expo_runtime_version" />
    <meta-data android:name="expo.modules.updates.UPDATES_CONFIGURATION_REQUEST_HEADERS_KEY" android:value="{&quot;expo-channel-name&quot;:&quot;production&quot;}" />

    <activity
      android:name="com.solcode.jirmalam.MainActivity"
      android:label="@string/app_name"
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:launchMode="singleTask"
      android:windowSoftInputMode="adjustPan"
      android:exported="true"
      android:theme="@style/BootTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="jirumalarm" />
      </intent-filter>
      <!-- App Links: jirum-alarm.com 링크를 브라우저 대신 앱이 연다.
           autoVerify 는 서버의 /.well-known/assetlinks.json 이 있어야 성립한다. -->
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" android:host="jirum-alarm.com" />
        <data android:scheme="https" android:host="www.jirum-alarm.com" />
      </intent-filter>
    </activity>
    <activity
      android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />

        <data
          android:host="oauth"
          android:scheme="kakaoa14549f2c54214ea2a05669c34a3f11f" />
      </intent-filter>
    </activity>
  </application>
</manifest>
