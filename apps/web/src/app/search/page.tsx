import SearchPageComponent from '../(home)/components/(search)/SearchPage';

export default function SearchPage() {
  return (
    <div className="mx-auto h-full max-w-screen-mobile-max px-5 pb-20">
      <SearchPageComponent />
    </div>
  );
}
